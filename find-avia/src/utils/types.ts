export type TicketDataType = {
  departureCity: string;
  arrivalCity: string;
  departureDate: string;
  returnDate: string;
  isRoundTrip: boolean;
};

export type TimesType = { fromTime: string; toTime: string };
